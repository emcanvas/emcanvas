# OpenSpec for EmCanvas

This repository uses two complementary layers:

- `spec/` for long-form product and architecture documentation.
- `openspec/` for standardized project/change artifacts.

The initial change, `define-emcanvas-product`, formalizes the product definition and architectural contract for EmCanvas. It intentionally does not include implementation-ready engineering subtasks.
