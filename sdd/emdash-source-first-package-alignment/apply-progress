## Implementation Progress

**Change**: emdash-source-first-package-alignment
**Mode**: Strict TDD

### Completed Tasks
- [x] 1.1 Expand package-surface and runtime contract tests so the public `emcanvas` exports RED on `dist/*`-first entrypoints or a parallel dev-source contract.
- [x] 1.2 Update the package/docs contract (`package.json`, README, and integration checklists/guides) so local EmDash consumption uses the source-first `emcanvas` package surface as the only public workflow.
- [x] 1.3 Remove the redundant dev-source descriptor path while preserving the native package-specifier descriptor contract for `emcanvas`, `emcanvas/sandbox`, `emcanvas/admin`, and `emcanvas/astro`.
- [x] 2.1 Run focused contract verification for package exports, descriptor alignment, and source-first docs boundaries.
- [x] 2.2 Save `sdd/emdash-source-first-package-alignment/apply-progress` with completion status and strict-TDD evidence.

### Files Changed
| File | Action | What Was Done |
|------|--------|---------------|
| `package.json` | Modified | Switched the public root and subpath exports to `src/plugin/*` entry modules while keeping `dist` as the packaged artifact boundary. |
| `README.md` | Modified | Replaced dist-first local-host guidance with the source-first repo-package workflow and bounded smoke handoff. |
| `docs/integration/emdash-dev-source-consumption.md` | Modified | Reframed local EmDash consumption around native `emcanvas` package specifiers and removed the alternate dev-source path from the public contract. |
| `docs/integration/emdash-consumable-package-checklist.md` | Modified | Locked the checklist to one canonical local dependency and source-first host resolution. |
| `docs/integration/emdash-plugin-runtime-checklist.md` | Modified | Realigned the runtime checklist with the native descriptor and source-first public surface. |
| `tests/contracts/package-runtime-exports.test.ts` | Modified | Asserted source-first root/subpath exports and `dist` as artifact-only. |
| `tests/contracts/package-surface.test.ts` | Modified | Locked the package surface to source-first runtime entrypoints with unchanged peer dependency expectations. |
| `tests/contracts/emdash-plugin-runtime.test.ts` | Modified | Preserved the single native descriptor contract and asserted root/subpath loading without a separate dev-source contract. |
| `tests/contracts/consumable-package-docs.test.ts` | Modified | Asserted one canonical local workflow and removed the old optional dev-source guidance from the docs contract. |
| `tests/contracts/path-alias-config.test.ts` | Modified | Reasserted that the documented import specifier is the public `emcanvas` package surface rather than a dev-source alias. |
| `src/plugin/dev-source.ts` | Deleted | Removed the redundant alternate dev-source descriptor entrypoint from the public workflow. |
| `src/plugin/runtime/create-dev-source-plugin-descriptor.ts` | Deleted | Removed the obsolete absolute-path descriptor generator. |
| `sdd/emdash-source-first-package-alignment/tasks` | Created | Restored the completed task checklist for this bounded archive-ready remediation. |
| `sdd/emdash-source-first-package-alignment/apply-progress` | Created | Restored the missing apply artifact with the required strict-TDD evidence table. |

### TDD Cycle Evidence
| Task | Test File | Layer | Safety Net | RED | GREEN | TRIANGULATE | REFACTOR |
|------|-----------|-------|------------|-----|-------|-------------|----------|
| 1.1 / 1.3 | `tests/contracts/package-runtime-exports.test.ts`, `tests/contracts/package-surface.test.ts`, `tests/contracts/emdash-plugin-runtime.test.ts` | Contract | ✅ Existing package/runtime contract files were used as the safety net before changing exports or descriptor wiring. | ✅ The export/descriptor expectations were tightened first so the suite failed while the package still exposed `dist/*`-first and dev-source-era behavior. | ✅ The focused package/runtime contract suite passed once `package.json` exports, descriptor wiring, and public loading aligned on `src/plugin/*`. | ✅ Coverage spans root export, subpath exports, native descriptor fields, and executable root/subpath package loading so the implementation cannot regress to split contracts. | ➖ None needed beyond removing the redundant dev-source path. |
| 1.2 / 2.1 | `tests/contracts/consumable-package-docs.test.ts`, `tests/contracts/path-alias-config.test.ts` | Docs contract | ✅ Existing docs contract files were kept green first to protect the published workflow wording. | ✅ The docs assertions were tightened first so they failed while README/integration guidance still mentioned the legacy dev-source path and non-canonical local workflow. | ✅ The focused docs contract suite passed after the README and integration docs consistently documented native `emcanvas` package imports as the only public local path. | ✅ Assertions now cover the README, consumable package checklist, dev-source guide boundaries, and the exact documented import specifier. | ➖ None needed; the change was bounded to wording and contract cleanup. |

### Test Summary
- **Total tests written/updated**: 5 focused contract files updated
- **Focused verification**: `pnpm vitest run tests/contracts/package-runtime-exports.test.ts tests/contracts/package-surface.test.ts tests/contracts/emdash-plugin-runtime.test.ts tests/contracts/consumable-package-docs.test.ts tests/contracts/path-alias-config.test.ts`
- **Result**: 5 test files passed, 17 tests passed
- **Layers used**: Contract
- **Approval tests** (refactoring): None — the work tightened existing package/docs contracts rather than refactoring runtime behavior.
- **Pure functions created**: 0

### Deviations from Design
None — the change keeps source-first public exports, a single native descriptor contract, and `dist/*` as an artifact-only boundary.

### Issues Found
None.

### Remaining Tasks
- [ ] None.

### Status
5/5 tasks complete. Ready for archive.
